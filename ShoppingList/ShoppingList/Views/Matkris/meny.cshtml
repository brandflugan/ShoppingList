<nav class="navbar navbar-default navbar-static-top menu">
    <div class="container-fluid">
        <div class="container">
            <div class="navbar-header">
                <a class="navbar-brand" href="index">matkris.se</a>
            </div>
            <ul class="nav navbar-nav">
                <li class="menu-item-click @if (Request.RawUrl.ToLower().Contains("privat")) {<text>active</text> }" id="menu-state-private"><a href="privat">Privat</a></li>
                <li class="menu-item-click @if (Request.RawUrl.ToLower().Contains("foretag")) {<text>active</text> }" id="menu-state-business"><a href="foretag">Företag</a></li>
                @if (Request.RawUrl.ToLower().Contains("upload"))
                {
                    using (Html.BeginForm("Logout", "Matkris", FormMethod.Post))
                    {
                    <li class="" id="menu-logout"><button class="btn btn-default btn-sm" type="submit">Logga ut</button></li>
                    }
                }
            </ul>
        </div>
    </div>
</nav>